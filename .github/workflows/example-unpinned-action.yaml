name: Demo - unpinned Action reference

# 手動専用の意図的な違反fixture。mergeしない。
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
