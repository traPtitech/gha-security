name: Example - current unpinned references
on:
  workflow_dispatch:
permissions: {}
jobs:
  fixture:
    runs-on: ubuntu-latest
    container: node:22-alpine
    services:
      postgres:
        image: postgres:16
    steps:
      - uses: actions/checkout@v4
      - uses: docker://alpine:3.20
